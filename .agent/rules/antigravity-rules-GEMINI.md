---
source: .agent/rules/GEMINI.md
type: antigravity
---

# Antigravity Runtime Rules

Source of truth: `.agent/rules/GEMINI.md`.

On a fresh Beats PM Kit URL-only setup, clone/open the repo and run:

```bash
python3 system/scripts/bootstrap.py --agent --non-interactive --repo-url <url>
```

After bootstrap, load `.agent/rules/GEMINI.md`, resolve slash commands from `.agent/workflows/`, and keep Obsidian MCP read/search/open-only.
