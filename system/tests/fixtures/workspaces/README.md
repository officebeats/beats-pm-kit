# Real-Use Workspace Fixtures

These fixtures are sanitized examples for scenario tests. Tests copy or build temp workspaces from these shapes and assert real artifact deltas instead of only checking that files exist.
